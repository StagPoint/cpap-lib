<UserControl x:Class="example_viewer.Controls.SignalChart"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:example_viewer.Controls"
             mc:Ignorable="d"
             d:DesignHeight="300" d:DesignWidth="300">
    <UserControl.Resources>
        <ResourceDictionary Source="SignalChartResources.xaml" />
    </UserControl.Resources>
    <Grid Name="ContainerGrid" Background="{TemplateBinding local:SignalChart.Background}">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"></ColumnDefinition>
            <ColumnDefinition Width="*"></ColumnDefinition>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition />
        </Grid.RowDefinitions>
        <TextBlock Grid.Row="0" Grid.Column="1" Name="CurrentValue" Padding="50 0"
                   Text="Current value information goes here">
        </TextBlock>
        <Label Grid.Row="1" Grid.Column="0" HorizontalContentAlignment="Center"
               VerticalContentAlignment="Center" VerticalAlignment="Center">
            <TextBlock Name="ChartLabel" Text="{TemplateBinding local:SignalChart.Label}"
                       Style="{TemplateBinding local:SignalChart.LabelStyle}">
                <TextBlock.LayoutTransform>
                    <RotateTransform Angle="-90" />
                </TextBlock.LayoutTransform>
                <TextBlock.RenderTransform>
                    <TranslateTransform Y="-20" />
                </TextBlock.RenderTransform>
            </TextBlock>
        </Label>
        <WpfPlot Grid.Row="1" Grid.Column="1" Name="Chart" HorizontalAlignment="Stretch" Height="250"></WpfPlot>
    </Grid>
</UserControl>