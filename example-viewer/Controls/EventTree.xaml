<UserControl x:Class="cpapviewer.Controls.EventTree"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:controls="clr-namespace:cpapviewer.Controls"
             xmlns:viewModels="clr-namespace:cpapviewer.ViewModels"
             xmlns:local="clr-namespace:cpapviewer"
             mc:Ignorable="d"
             d:DesignHeight="800" d:DesignWidth="350">
    <UserControl.Resources>
        <Style TargetType="{x:Type TreeViewItem}" BasedOn="{StaticResource {x:Type TreeViewItem}}">
            <Setter Property="IsExpanded" Value="True" />
        </Style>
        <local:FormattedTimespanConverter x:Key="FormattedTimeSpan" />
        <local:EventTypeToNameConverter x:Key="EventTypeToName" />
        <ResourceDictionary x:Key="CompactSpacing" Source="pack://application:,,,/ModernWpf;component/DensityStyles/Compact.xaml" />
    </UserControl.Resources>
    <StackPanel Orientation="Vertical">
        <TreeView
            x:Name="tvwEvents"
            VerticalAlignment="Top"
            HorizontalAlignment="Stretch"
            ItemsSource="{Binding Path=Items}" d:DataContext="{viewModels:DailyEventsViewModel}" Margin="0 10">
            <TreeView.ItemTemplate>
                <HierarchicalDataTemplate ItemsSource="{Binding Events}">
                    <TextBlock FontWeight="Bold">
                        <Run Text="{Binding Type, Mode=OneWay, Converter={StaticResource EventTypeToName}}" />
                        <!--<Run Text="{Binding TotalCount, Mode=OneWay, StringFormat=({0:N1})}" />-->
                    </TextBlock>
                    <HierarchicalDataTemplate.ItemTemplate>
                        <DataTemplate>
                            <StackPanel Orientation="Horizontal">
                                <TextBlock Text="{Binding StartTime, Mode=OneWay, StringFormat={}{0:T}}" Width="100" />
                                <TextBlock Text="{Binding Duration, Mode=OneWay, Converter={StaticResource FormattedTimeSpan}, ConverterParameter=Short.Empty}"/>
                            </StackPanel>
                        </DataTemplate>
                    </HierarchicalDataTemplate.ItemTemplate>
                </HierarchicalDataTemplate>
            </TreeView.ItemTemplate>
        </TreeView>
    </StackPanel>
</UserControl>